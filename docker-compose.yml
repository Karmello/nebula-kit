networks:
  default:
    name: neb_net
services:
  neb-kit:
    container_name: neb-kit
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - PAT=$PAT
    ports:
      - 5173:5173
    environment:
      - NODE_ENV=$NODE_ENV
