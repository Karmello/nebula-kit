import { type Changelog } from 'client/definitions'

export const FLEX_CHANGELOG: Changelog = {
  '0.9.0': [
    'exposed `hidden` prop on Flex.Item via Box',
    'added support for predefined size scale values on gap-related props',
  ],
  '0.2.3': ['released'],
}
