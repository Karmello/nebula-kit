import { type Changelog } from 'client/definitions'

export const SPACER_CHANGELOG: Changelog = {
  '0.9.0': ['added support for predefined size scale values on the `blockSize` prop'],
  '0.2.3': ['released'],
}
