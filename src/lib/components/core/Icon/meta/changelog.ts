import { type Changelog } from 'client/definitions'

export const ICON_CHANGELOG: Changelog = {
  '0.10.0': ['made `name` and `size` props responsive'],
  '0.9.0': ['added support for predefined size scale values on the `size` prop'],
  '0.2.3': ['released'],
}
