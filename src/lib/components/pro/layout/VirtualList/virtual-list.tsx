import { Box } from 'lib/components'

import { VirtualListProps } from './definitions'

export const VirtualList = (props: VirtualListProps) => {
  return <Box />
}

VirtualList.displayName = 'VirtualList'
