import { PasswordInputProps } from './types'

export const DEFAULT_PASSWORD_INPUT_SCALE: PasswordInputProps['scale'] = 'md'

export const DEFAULT_PASSWORD_INPUT_AUTO_COMPLETE: PasswordInputProps['autoComplete'] =
  'current-password'
