{
  "name": "IconButton",
  "description": "IconButton is a specialized button for icon-only actions. It inherits all button styling (variants, intents, sizes, disabled state) but omits children, always rendering a single icon centered within a square button.",
  "props": [
    {
      "name": "iconName",
      "type": "ButtonOwnProps['iconName']",
      "options": [
        "check",
        "close",
        "search",
        "menu",
        "panel-left-open",
        "panel-right-open",
        "chevron-down",
        "chevron-up",
        "chevron-left",
        "chevron-right"
      ],
      "required": true,
      "defaultValue": "",
      "description": "Icon name to render inside the button"
    },
    {
      "name": "variant",
      "type": "ButtonOwnProps['variant']",
      "options": [
        "solid",
        "outline",
        "ghost"
      ],
      "required": false,
      "defaultValue": "'solid'",
      "description": "Visual style variant of the button (e.g. solid, outline, ghost)"
    },
    {
      "name": "intent",
      "type": "ButtonOwnProps['intent']",
      "options": [
        "neutral",
        "primary",
        "secondary",
        "tertiary",
        "success",
        "info",
        "warning",
        "danger",
        "inverse"
      ],
      "required": false,
      "defaultValue": "'neutral'",
      "description": "Semantic intent for coloring (e.g. primary, success, danger, neutral)"
    },
    {
      "name": "disabled",
      "type": "ButtonOwnProps['disabled']",
      "options": [],
      "required": false,
      "defaultValue": "false",
      "description": "Disables interaction and applies disabled styling"
    },
    {
      "name": "size",
      "type": "ButtonOwnProps['size']",
      "options": [
        "sm",
        "md",
        "lg"
      ],
      "required": false,
      "defaultValue": "'md'",
      "description": "Button size controlling height and font size"
    },
    {
      "name": "borderRadius",
      "type": "ButtonOwnProps['borderRadius']",
      "options": "0, 1, 2 ... 80",
      "required": false,
      "defaultValue": "",
      "description": ""
    }
  ]
}