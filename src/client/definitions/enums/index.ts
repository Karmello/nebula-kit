export enum RoutingCategoryKey {
  primitive = 'primitive',
  controls = 'controls',
  elements = 'elements',
  utility = 'utility',
}

export enum RoutingItemKey {
  box = 'box',
  flex = 'flex',
  grid = 'grid',
  text = 'text',
  button = 'button',
  nebKitProvider = 'neb-kit-provider',
}
