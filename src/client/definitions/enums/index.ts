export enum RoutingCategoryKey {
  primitive = 'primitive',
  controls = 'controls',
  elements = 'elements',
  layout = 'layout',
  utility = 'utility',
}

export enum RoutingItemKey {
  box = 'box',
  flex = 'flex',
  grid = 'grid',
  text = 'text',
  button = 'button',
  stack = 'stack',
  hstack = 'h-stack',
  vstack = 'v-stack',
  nebKitProvider = 'neb-kit-provider',
}
