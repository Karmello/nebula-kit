import { Text } from 'lib/components'

import data from 'client/meta/side-panel-layout.meta'

const SidePanelLayoutOverviewPage = () => {
  return <Text>{data.description}</Text>
}

export default SidePanelLayoutOverviewPage
