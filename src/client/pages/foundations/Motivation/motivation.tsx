import { Callout } from 'lib/components'

export default () => {
  return <div />
  // return (
  //   <Callout heading="Simplicity first">
  //     Nebula-kit avoids extra abstraction layers or over-engineering. Props map directly to CSS, components
  //     stay close to native HTML, and the system favors clarity over cleverness - while still giving you enough
  //     power to build functional, responsive interfaces.
  //   </Callout>
  // )
}
