import { Box, Section } from 'lib/components'

export const RecoveryPage = () => {
  return (
    <Box padding={{ base: 20, lg: 50 }}>
      <Box inlineSize="400px" maxInlineSize="100%" margin="0 auto">
        <Section heading="Password recovery" iconName="user-plus">
          ...
        </Section>
      </Box>
    </Box>
  )
}
