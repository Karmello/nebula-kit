import { type DocChangelog } from 'client/definitions'

export const VIRTUAL_LIST_CHANGELOG: DocChangelog = {
  '0.11.0': [
    'changed `itemHeight` prop to `itemBlockSize`',
    'changed `surface` prop to `surfaceDepth`',
  ],
  '0.3.0': ['released'],
}
