import { type DocChangelog } from 'client/definitions'

export const TABS_CHANGELOG: DocChangelog = {
  '0.11.0': ['changed `orientation` prop to `direction`'],
  '0.10.0': ['changed `flexDirection` prop to `orientation`'],
  '0.3.0': ['released'],
}
