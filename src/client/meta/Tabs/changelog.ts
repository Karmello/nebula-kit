import { type Changelog } from 'client/definitions'

export const TABS_CHANGELOG: Changelog = {
  '0.11.0': ['changed `orientation` prop to `direction`'],
  '0.10.0': ['changed `flexDirection` prop to `orientation`'],
  '0.3.0': ['released'],
}
