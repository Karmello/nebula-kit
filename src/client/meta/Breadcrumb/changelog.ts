import { type DocChangelog } from 'client/definitions'

export const BREADCRUMB_CHANGELOG: DocChangelog = {
  '0.7.0': ['removed `itemBorderIntent` prop'],
  '0.6.0': ['added `itemBorderIntent` prop'],
  '0.2.3': ['released'],
}
