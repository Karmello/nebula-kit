import { type DocChangelog } from 'client/definitions'

export const DIALOG_CHANGELOG: DocChangelog = {
  '0.6.0': ['fixed dialog closing on inside clicks when `closeOnBackdropClick` was enabled'],
  '0.5.0': ['fixed backdrop flicker'],
  '0.2.3': ['released'],
}
