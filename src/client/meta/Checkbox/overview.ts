import type { DocOverview } from 'client/definitions'

export const CHECKBOX_OVERVIEW: DocOverview = {
  bundle: 'core',
  title: 'Form control for toggling a binary on/off state.',
  features: ['supports both controlled and uncontrolled modes'],
  composedOf: ['Box', 'Icon'],
  exposedTags: ['div'],
}
