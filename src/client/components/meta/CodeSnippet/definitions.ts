export const COLOR_MAP = {
  // background
  bg: 'hsl(var(--neb-gray-1))',
  // component name
  '#F78C6C': 'hsl(var(--neb-red-6))',
  // native html tag name
  '#CAECE6': 'hsl(var(--neb-red-6))',
  // prop name
  '#C5E478': 'hsl(var(--neb-blue-6))',
  // object name, object key names
  '#D6DEEB': 'hsl(var(--neb-gray-6))',
  // value
  '#ECC48D': 'hsl(var(--neb-amber-6))',
  // param name
  '#D7DBE0': 'hsl(var(--neb-amber-6))',
  // argument name
  '#FF5874': 'hsl(var(--neb-amber-6))',
  // called func name
  '#82AAFF': 'hsl(var(--neb-blue-6))',
  // TS type name
  '#FFCB8B': 'hsl(var(--neb-gray-12))',
  // angle brackets, cb curly brackets
  '#7FDBCA': 'hsl(var(--neb-gray-12))',
  // prop curly brackets
  '#D3423E': 'hsl(var(--neb-gray-12))',
  // equal sign, arrow func sign, dot
  '#C792EA': 'hsl(var(--neb-gray-12))',
  // quotes
  '#D9F5DD': 'hsl(var(--neb-gray-12))',
  // comments
  '#637777': 'hsl(var(--neb-gray-12))',
  // extension
  '#5CA7E4': 'hsl(var(--neb-blue-6))',
  //
  '#BAEBE2': 'hsl(var(--neb-blue-6))',
}
